(function (window) {
  'use strict';

  window.NPUK_GOOGLE_CONFIG = {
    // Paste one of these public IDs when Google is ready.
    // GA4 example: G-XXXXXXXXXX
    ga4MeasurementId: 'G-SLBR6QZ84H',
    // GTM example: GTM-XXXXXXX. If this is set, it takes priority over GA4 direct.
    gtmContainerId: '',
  };
}(window));
