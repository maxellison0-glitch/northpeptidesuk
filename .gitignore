# Dependencies (dev tooling only — never shipped to the browser)
node_modules/

# Environment / secrets
.env
.env.local
.env.*.local

# Build / tooling caches
tools/.cache/
.superpowers/
.netlify/
.vercel/

# Local agent/tooling config
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Internal business data (margins, supplier costs, etc.) — this repo is public,
# never commit anything here.
internal/
